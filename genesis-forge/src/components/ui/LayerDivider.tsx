/**
 * Layer divider — the site's recurring structural motif. Thin stacked lines
 * evoke FDM print layers (true to the subject), used to separate major bands.
 * Purely decorative, so hidden from assistive tech.
 */
export function LayerDivider() {
  return <div className="gf-layer-divider" aria-hidden="true" />;
}
